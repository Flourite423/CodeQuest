# CodeQuest 部署说明

> **项目**: CodeQuest - 编程学习平台  
> **版本**: v1.0  
> **日期**: 2026-05-28

---

## 一、项目概述

CodeQuest 是一个面向编程学习者的三端学习应用，包含：

| 端 | 技术栈 | 用途 | 默认端口 |
|---|--------|------|---------|
| **Backend** | Rust + Salvo + SQLx + PostgreSQL | REST API 服务 | 3001 |
| **Mobile** | Flutter 3.x (Web 构建) | 学习者客户端 | 8088 |
| **Admin** | Vue 3 + Element Plus (构建产物) | 管理后台 | 3000 |

---

## 二、交付物说明

本次交付包含以下文件：

| 文件名 | 说明 | 大小 |
|--------|------|------|
| `CodeQuest-源代码.zip` | 完整源代码（不含构建产物和依赖） | ~38 MB |
| `CodeQuest-源程序.zip` | 编译后的可运行程序 | ~104 MB |
| `部署说明.md` | 本文件 | - |

### 2.1 源代码包内容

```
CodeQuest-源代码/
├── backend/          # Rust 后端源代码
│   ├── src/          # 源代码
│   ├── migrations/   # 数据库迁移脚本
│   ├── config/       # 配置文件
│   └── tests/        # 集成测试
├── mobile/           # Flutter 客户端源代码
│   ├── lib/          # Dart 源代码
│   ├── assets/       # 静态资源
│   └── web/          # Web 配置
├── admin/            # Vue 3 管理后台源代码
│   ├── src/          # TypeScript/Vue 源代码
│   └── ...
├── contracts/        # API 契约文档
│   └── openapi/      # OpenAPI 3.0 规范
└── doc/              # 项目文档
```

### 2.2 源程序包内容

```
CodeQuest-Program/
├── backend/
│   └── learning-app-backend    # 后端可执行文件 (Linux x64)
├── mobile/
│   └── ...                     # Flutter Web 构建产物
└── admin/
    └── ...                     # Vue 3 构建产物 (静态文件)
```

---

## 三、系统要求

### 3.1 服务器环境

| 组件 | 最低版本 | 说明 |
|------|---------|------|
| **操作系统** | Linux (Ubuntu 20.04+) | 推荐 Ubuntu 22.04 LTS |
| **PostgreSQL** | 15+ | 数据库服务 |
| **Nginx** | 1.18+ | 反向代理 / 静态文件服务 |
| **内存** | 2 GB | 建议 4 GB 以上 |
| **磁盘** | 10 GB | 建议 20 GB 以上 |

### 3.2 开发环境（如需从源码构建）

| 组件 | 版本 | 用途 |
|------|------|------|
| **Rust** | 1.75+ | 后端编译 |
| **Node.js** | 18+ | 前端构建 |
| **Flutter** | 3.x | 移动端构建 |
| **sqlx-cli** | 最新版 | 数据库迁移 |

---

## 四、部署步骤

### 4.1 数据库部署

#### 4.1.1 安装 PostgreSQL

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install postgresql postgresql-contrib

# 启动服务
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

#### 4.1.2 创建数据库

```bash
# 切换到 postgres 用户
sudo -u postgres psql

# 在 psql 中执行
CREATE DATABASE learning_app;
CREATE USER codequest WITH PASSWORD 'your-secure-password';
GRANT ALL PRIVILEGES ON DATABASE learning_app TO codequest;
\q
```

#### 4.1.3 执行数据库迁移

**方式一：使用 sqlx-cli（推荐）**

```bash
# 安装 sqlx-cli
cargo install sqlx-cli --no-default-features --features native-tls,postgres

# 进入后端源码目录
cd backend

# 设置数据库 URL
export DATABASE_URL="postgres://codequest:your-secure-password@localhost/learning_app"

# 执行迁移
sqlx migrate run
```

**方式二：直接执行 SQL 文件**

```bash
# 按顺序执行 migrations 目录下的 SQL 文件
sudo -u postgres psql -d learning_app -f backend/migrations/001_initial_schema.sql
sudo -u postgres psql -d learning_app -f backend/migrations/002_increase_refresh_token_length.sql
# ... 依次执行所有迁移文件
```

#### 4.1.4 导入种子数据（可选）

```bash
# 导入初始测试数据
sudo -u postgres psql -d learning_app -f backend/seed_data.sql
```

### 4.2 后端服务部署

#### 4.2.1 使用预编译二进制（源程序包）

```bash
# 解压源程序包
unzip CodeQuest-源程序.zip
cd CodeQuest-Program/backend

# 赋予执行权限
chmod +x learning-app-backend

# 设置环境变量
export APP__SERVER_ADDR="0.0.0.0:3001"
export APP__DATABASE_URL="postgres://codequest:your-secure-password@localhost/learning_app"
export APP__JWT_SECRET="your-jwt-secret-minimum-32-characters-long"
export APP__JWT_EXPIRATION="86400"
export APP__AUTO_RUN_MIGRATIONS="false"
export APP__SEED_DEV_ACCOUNTS="false"

# AI 配置（可选）
export APP__AI__PROVIDER="mock"  # 或 "deepseek"
export APP__AI__API_KEY=""
export APP__AI__MODEL="deepseek-chat"

# 启动服务
./learning-app-backend
```

#### 4.2.2 使用 systemd 管理（生产环境推荐）

创建服务文件 `/etc/systemd/system/codequest-backend.service`：

```ini
[Unit]
Description=CodeQuest Backend Service
After=network.target postgresql.service

[Service]
Type=simple
User=codequest
WorkingDirectory=/opt/codequest/backend
ExecStart=/opt/codequest/backend/learning-app-backend
Restart=on-failure
RestartSec=5

# 环境变量
Environment="APP__SERVER_ADDR=0.0.0.0:3001"
Environment="APP__DATABASE_URL=postgres://codequest:your-secure-password@localhost/learning_app"
Environment="APP__JWT_SECRET=your-jwt-secret-minimum-32-characters-long"
Environment="APP__JWT_EXPIRATION=86400"
Environment="APP__AI__PROVIDER=mock"

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable codequest-backend
sudo systemctl start codequest-backend

# 查看状态
sudo systemctl status codequest-backend

# 查看日志
sudo journalctl -u codequest-backend -f
```

### 4.3 前端部署

#### 4.3.1 管理后台 (Admin)

管理后台为纯静态文件，可使用 Nginx 或任何静态文件服务器托管。

**使用 Nginx：**

```nginx
server {
    listen 3000;
    server_name admin.your-domain.com;
    
    root /opt/codequest/admin;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 4.3.2 移动端 Web (Mobile)

```nginx
server {
    listen 8088;
    server_name app.your-domain.com;
    
    root /opt/codequest/mobile;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 4.3.3 使用 Python 快速预览（测试用）

```bash
# 管理后台
cd CodeQuest-Program/admin
python3 -m http.server 3000

# 移动端（另开终端）
cd CodeQuest-Program/mobile
python3 -m http.server 8088
```

### 4.4 Nginx 完整配置示例

```nginx
# /etc/nginx/sites-available/codequest

# 后端 API
upstream codequest_backend {
    server 127.0.0.1:3001;
}

# 管理后台
server {
    listen 80;
    server_name admin.your-domain.com;
    
    root /opt/codequest/admin;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://codequest_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 移动端
server {
    listen 80;
    server_name app.your-domain.com;
    
    root /opt/codequest/mobile;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://codequest_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

启用配置：

```bash
sudo ln -s /etc/nginx/sites-available/codequest /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

## 五、环境变量配置

### 5.1 后端环境变量

所有环境变量以 `APP__` 为前缀，使用双下划线表示层级：

| 变量名 | 必填 | 默认值 | 说明 |
|--------|------|--------|------|
| `APP__SERVER_ADDR` | 是 | `0.0.0.0:3001` | 服务监听地址 |
| `APP__DATABASE_URL` | 是 | - | PostgreSQL 连接字符串 |
| `APP__JWT_SECRET` | 是 | - | JWT 签名密钥（至少32位） |
| `APP__JWT_EXPIRATION` | 否 | `86400` | Token 过期时间（秒） |
| `APP__AUTO_RUN_MIGRATIONS` | 否 | `false` | 启动时自动执行迁移 |
| `APP__SEED_DEV_ACCOUNTS` | 否 | `false` | 启动时导入测试账号 |
| `APP__AI__PROVIDER` | 否 | `mock` | AI 提供商：mock/deepseek |
| `APP__AI__API_KEY` | 否 | - | DeepSeek API 密钥 |
| `APP__AI__MODEL` | 否 | `deepseek-chat` | AI 模型名称 |
| `APP__AI__TEMPERATURE` | 否 | `0.3` | 生成温度 |
| `APP__AI__MAX_TOKENS` | 否 | `500` | 最大 Token 数 |

### 5.2 配置文件

后端也支持通过 TOML 配置文件进行配置：

```toml
# config/default.toml
server_addr = "0.0.0.0:3001"
database_url = "postgres://user:pass@localhost/learning_app"
jwt_secret = "your-secret-key"
jwt_expiration = 86400
auto_run_migrations = false
seed_dev_accounts = false

[ai]
provider = "mock"
api_key = ""
model = "deepseek-chat"
temperature = 0.3
max_tokens = 500
```

---

## 六、从源码构建

如需从源代码重新构建，请参考以下步骤：

### 6.1 后端构建

```bash
cd backend

# 开发构建
cargo build

# 生产构建（优化）
cargo build --release

# 运行测试
cargo test

# 代码检查
cargo clippy
```

### 6.2 管理后台构建

```bash
cd admin

# 安装依赖
npm install

# 开发运行
npm run dev

# 生产构建
npm run build

# 代码检查
npm run lint
```

### 6.3 移动端构建

```bash
cd mobile

# 获取依赖
flutter pub get

# 开发运行（Chrome）
flutter run -d chrome

# Web 生产构建
flutter build web --release

# 构建产物位于 build/web/
```

---

## 七、验证部署

### 7.1 健康检查

```bash
# 检查后端服务
curl http://localhost:3001/api/v1/health

# 预期响应
{"data":"ok","meta":{"request_id":"...","timestamp":"..."}}
```

### 7.2 测试登录

```bash
# 学习者登录
curl -X POST http://localhost:3001/api/v1/auth/learner/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138000","password":"password123"}'

# 管理员登录
curl -X POST http://localhost:3001/api/v1/auth/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 7.3 前端访问

- 管理后台: http://localhost:3000
- 移动端: http://localhost:8088

---

## 八、常见问题

### 8.1 数据库连接失败

**症状**: 后端启动时报数据库连接错误

**解决**:
1. 检查 PostgreSQL 服务是否运行：`sudo systemctl status postgresql`
2. 检查数据库和用户是否存在
3. 检查连接字符串格式是否正确
4. 检查防火墙设置

### 8.2 跨域问题

**症状**: 前端无法调用 API

**解决**: 后端已内置 CORS 支持，确保前端请求的 `Origin` 在允许列表中。如需修改，请查看后端配置。

### 8.3 端口冲突

**症状**: 启动时报 "Address already in use"

**解决**:
```bash
# 查看端口占用
sudo lsof -i :3001

# 终止占用进程
sudo kill -9 <PID>
```

### 8.4 权限问题

**症状**: 后端无法绑定端口或读写文件

**解决**:
```bash
# 确保二进制文件有执行权限
chmod +x learning-app-backend

# 如需使用 80/443 端口，需 root 权限或使用 systemd
```

---

## 九、目录结构参考

生产环境推荐目录结构：

```
/opt/codequest/
├── backend/
│   ├── learning-app-backend    # 可执行文件
│   └── .env                    # 环境变量文件
├── admin/                      # 管理后台静态文件
│   ├── index.html
│   └── assets/
├── mobile/                     # 移动端静态文件
│   ├── index.html
│   └── assets/
└── logs/                       # 日志目录
```

---

## 十、联系方式

如有部署问题，请参考：
- 项目文档：`doc/` 目录
- API 文档：`contracts/openapi/openapi.yaml`
- 后端规范：`backend/AGENTS.md`

---

**注意**: 生产环境部署时，请务必：
1. 修改默认的 JWT 密钥
2. 使用强密码
3. 配置 HTTPS
4. 设置防火墙规则
5. 定期备份数据库
